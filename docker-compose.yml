services:

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: ticket-flix-vue-bun
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: ticket-flix-laravel-php
    ports:
      - "8000:8000"